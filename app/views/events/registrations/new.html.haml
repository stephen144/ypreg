%h2 New Registration
%em Register for #{event.name}
.col-md-8
  .widget
    .widget-header
      %h3
        %i.fa.fa-edit
    .widget-content
      = render partial: 'form', locals: {event: event, registration: registration, event_lodgings: event_lodgings}
